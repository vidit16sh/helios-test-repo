name: Failing Workflow for Helios Test
on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: This step is designed to fail
        run: |
          echo "This workflow will fail and trigger the Helios CI GitHub App."
          exit 1